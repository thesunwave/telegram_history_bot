# Local QuestDB configuration with compression enabled
# Max number of threads for compression during replication
replication.primary.compression.threads=2
# Compression level for zstd (1-22)
replication.primary.compression.level=3
